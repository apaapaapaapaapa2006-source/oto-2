# Oto II — 制作メモ / working conventions

カメラとマイクで遊ぶ、1ファイル完結の HTML 作品ギャラリー。
`main` に push すると GitHub Actions が Pages へ自動デプロイする。

## カテゴリ（2026-09-21 変更）

**新作は エフェクト ×2 本。楽器も新規は打ち止め。**
オーナーの指示で 2026-09-21 の回から適用（2026-09-14 変更の「楽器×1 + エフェクト×1」
から上書き）。既存のゲーム・楽器作品はそのまま残すので、`index.html` の
`<!-- GAME-CARDS-START/END -->` / `<!-- INSTRUMENT-CARDS-START/END -->` マーカーも、
`works.csv` のゲーム・楽器行も、xlsx のゲーム・楽器タブも消さないこと。
新しいカードを足さないだけ。

## いちばん優先する方針（2026-09-23 オーナーの声）── **撮って見せたくなるもの**

オーナーの評価：「最近あんまり面白くない」。気に入っているのは次の三本。

- **MADOKAGAMI** ── 顔と手が、青空の丘の壁紙の上で小さな OS ウィンドウの群れになる
- **ZANZOU** ── 今の自分が白黒の線画になり、少し前の自分が青・マゼンタ・黄の影で追いかけてくる
- **KORIN** ── リアム・ギャラガー風のタンバリン帽子が天から頭に降ってくる

**三本とも、オーナーが自分で持ち込んだファイル**で、毎晩の自動制作から生まれた作品ではない。
自動制作の作品は「まだ誰も使っていない機構」を探し、物理を厳密に解いて、長文で説明する方向に
寄っていた（SUIMEI の集光ヤコビアン、NARUKAMI の階段状先駆など）。正しいが、**遊んだ人が笑わない。**

三本に共通しているのはこれ。新作は**この節を 09-17 の四条件より優先**する。

1. **3 秒でわかる。** 説明文を読まずに、映った瞬間に何が起きているか分かり、「かっこいい」と言う。
   開始画面やカードに理屈の説明が要るなら、その案は捨てる。
2. **主役は映っている人。** 顔・頭・体・手の位置に何かが乗る・貼りつく・分身する・着せ替わる。
   部屋全体を物理シミュレーションの素材にする作品（水面・炎・氷・砂…）はもう十分ある。
3. **ポップな元ネタと、平面的で強いグラフィック。** OS の UI、ミュージックビデオ、ロックスター、
   Y2K、雑誌、ステッカー、プリクラ、ゲーム画面。ベタ塗り・原色・はっきりした線。
   上品な和の素材感・渋い暗色・微妙なシミュレーションは主役にしない。
4. **SNS に上げたくなる。** 画面いっぱい、⚙ で隠せる調整パネル、できれば録画して保存（KORIN の
   MediaRecorder 実装を参考に）。「撮って友だちに送りたいか」を毎回自問する。
5. **機構は単純でいい。** セグメンテーション、FaceLandmarker、時間差マスク、スプライトを顔に追従、
   くらいで十分面白くなる。新規性の確認は、ネタが面白いと決まった**あと**にやる。
6. **身振りのテンプレートを使わない。** 「手をかざす／握る／両手を高く掲げて一撃」をここ数十本
   ほぼ全部でなぞっている。一撃が要るなら作品ごとに違う引き金にする（顔の向き、口を開ける、
   ウインク、ジャンプ、二人並ぶ、タップ、音など）。無理に一撃を入れなくてもよい。
7. **出す前に見た目を目で確かめる。** この環境には Chromium と Playwright がある
   （`/opt/pw-browsers`）。`--use-fake-device-for-media-stream` か合成キャンバスの映像で実際に
   描かせてスクリーンショットを撮り、見て、直してから出す。数値の検証だけで出さない。

**目指すのは「かっこいい」であって「笑える」ではない（2026-09-23 追記）。**
試作の TELOP（表情にバラエティ番組のテロップと効果音がつく）はオーナー評価「よくない」。
理由は「方向がちがう：笑いより、かっこいい映像がほしい」。TELOP は文字を上に貼っただけで、
映像そのものはほとんど変わらず、主役の見た目がかっこよく化ける瞬間が無かった。だから次は:

- **映っている人の見た目そのものを、キマった映像に変える。** ZANZOU（線画＋色ずれの分身）、
  MADOKAGAMI（顔と手が窓の群れに）、KORIN（光輪の帽子）のように、画面を止めて一枚絵にしても
  かっこいいこと。スクリーンショットを見て「ジャケット・ポスター・MV のワンカットに使えるか」で判定する。
- **ギャグ、バラエティ、ツッコミ文字、ダジャレ、効果音主体の笑いは主役にしない。**
  文字やステッカーを貼るだけの作品も不可。文字を使うなら、グラフィックデザインとして使う。
- 方向の例：MV の映像処理（ストロボ分身、色版ずれ、グリッチを格好よく）、アニメの作画
  （セル塗り・オーラ・残光）、ライブの照明（レーザー・スポットが体の輪郭を追う）、
  ファッション誌やレコードジャケットのグラフィック、ホログラム・サイバー風の体。

説明文（works.csv・カード・台帳）も短く、何が起きるかだけを一〜二文で書く。数式や検証項目数は書かない。

## 方向性（2026-09-17 変更）── **ワクワクを最優先に**

オーナーから「最近あんまり面白味に欠ける。チームラボみたいにワクワクする
ものを」という指示が出た。原因ははっきりしていて、ここしばらくの作品が
**「ひとつの機構を厳密に作り込む」方向に寄りすぎ**て、見た瞬間に息を呑む
という軸が抜けていた。**技巧の正しさは前提であって、目的ではない。**

新作は次の四つのうち最低三つを満たすこと。設計を決める前にこの章へ戻る。

1. **物量** ── 数百〜数千の要素が同時に生きている。上品に3個ではなく、
   画面が埋まる量。固定プールと自動減量で実時間を担保したうえで、
   **量そのものを見せ場にする。**
2. **循環** ── 生まれて、育って、死んで、また生まれる。静止画の加工では
   なく、放っておいても動きつづける系にする。だから同じ瞬間が二度と来ない。
3. **痕跡** ── その人がそこに居たことが画面に残り、ゆっくり消えていく。
   操作の結果が一瞬で消えるなら、それは体験ではなく反応でしかない。
4. **一撃** ── 溜めて放つ大きな出来事をひとつ用意する。全画面のフラッシュ、
   衝撃波、全要素の一斉放出。作品につき一回だけ許される派手さ。

あわせて:

- **暗い画面に高彩度の発光**は、これまで避けてきたぶん強い武器になる。
  「一晩に暗い作品は一本まで」の制約は 楽器＋エフェクトの2本立てでの話で、
  ワクワク優先の作品なら暗いほうを選んでよい。
- **加算合成のブルーム**（縮小コピーを `lighter` で重ねる）を惜しまない。
  費用は drawImage 二回で、見た目の格が一段変わる。
- 音は付け足しではなく**作品そのものが鳴っている**状態にする。
  RYORAN は花が一輪開くたびに一音鳴るので、庭が育つことがそのまま音楽になる。
- ただし**検証の水準は一切下げない。** 派手さは、壊れないことと両立させる。

参照実装は `ryoran/`（2026-09-17）。

## 毎回の作業でやること

新しい作品を追加したら、**必ず 2 か所**を更新する:

1. `index.html` — 該当セクションのマーカーの間にカードを 1 枚追加
   (`<!-- GAME-CARDS-START/END -->` / `INSTRUMENT` / `EFFECT`)。
   `<main>` 冒頭のコメントにあるテンプレートをそのまま複製すること
   （2026-09-21 にカードを `<article class="card" data-date="…" data-cat="…"
   data-slug="…">` 構造へ作り替えた。`data-date` は `works.csv` に足す日付と
   同じ JST の値、`data-slug` はフォルダ名）。
   セクションの件数・検索/絞り込み/並び替え・お気に入り☆・メモ✎・
   「まだ作品がありません」の出し分けはすべて JS が自動でやる
   （お気に入りとメモは各人のブラウザの localStorage に保存されるだけで、
   このリポジトリやオーナー間では共有されない）。

2. `works.csv` — 1 作品 1 行を追記する。列は
   `日付,カテゴリ,名前,説明,URL`。
   日付は JST の制作日 (このリポジトリの定期実行は毎晩 2:00 JST なので、
   UTC のコミット時刻から 9 時間進めた日付になる点に注意)。
   説明はカンマを含めない（含める場合は行全体を `"` で囲む）。

### 作品台帳（Artifact）も一緒に更新する

オーナー用の一覧ページを Artifact として公開している。**同じ URL を更新し続けること。**

    https://claude.ai/code/artifact/76c24e8c-8a85-4af0-aeae-0ea369543780

ソースは `tools/register.artifact.html`（このリポジトリ内）。手順:

1. `tools/register.artifact.html` の `WORKS` 配列の**先頭**に今夜の作品を追加する
   （`date` は JST、`key` は `game` / `inst` / `fx`)。
2. Artifact ツールを `url` に上記 URL を指定して呼び、このファイルを publish する。
   `url` を省くと**別の Artifact が新規作成されてしまう**ので必ず指定する。
   publish 前に `action:"read"` でその URL を読むこと（読まずに publish すると拒否される）。
3. `favicon` は再指定しない（🗂 のまま維持される）。

集計・絞り込み・TSV 出力はページ側の JS が `WORKS` から自動生成するので、
配列に 1 件足すだけでよい。

### スプレッドシート（oto-2-works.xlsx）

`works.csv` から 3 タブ（ゲーム / 楽器 / エフェクト）の .xlsx を作るスクリプトがある。
Google ドライブへ入れればそのまま Google スプレッドシートになる。

    python3 tools/make_xlsx.py            # works.csv -> oto-2-works.xlsx
    python3 tools/make_xlsx.py 別名.xlsx   # 出力先を指定

列は `no ・ 〇/△ ・ 日付 ・ 名前 ・ 簡単な説明 ・ memo ・ ジャンル`。
**`〇/△` と `memo` はオーナーが手で書く列**で、作り直すときは既存ファイルから
作品名を鍵にして引き継がれる。だから作品を追加した夜は

1. `works.csv` に行を足す（従来どおり）
2. オーナーがドライブから `.xlsx` で書き出してリポジトリに置いてあれば、
   `python3 tools/make_xlsx.py` を実行して作り直す

の順で、オーナーの書き込みを消さずに新作を足せる。置いていなければ新規生成でよい
（その場合 `〇/△` は全部 `〇`、`memo` は空になる）。

実行環境から docs.google.com へは到達できない（egress proxy が接続を拒否する）ので、
**シートを直接編集することはできない。** .xlsx を渡すのが唯一の経路。

### works.csv は何のためにあるか

オーナーが管理しているスプレッドシートが、この CSV を
`=IMPORTDATA("https://apaapaapaapaapa2006-source.github.io/oto-2/works.csv")`
で直接読み込んでいる。**この行を追記し忘れると、スプレッドシート側に
その作品が出てこない。** 実行環境からは docs.google.com へ到達できない
(egress proxy が 403 を返す) ため、シートを直接編集することはできない。
CSV を更新して push することが、シートを更新する唯一の方法。

列の並びや列名を変えると、シート側の見出しとずれるので変えないこと。

## 作品ファイルの決まり

- 1 作品 = 小文字ケバブケースのフォルダ + `index.html` 1 枚。ビルド無し。
- UI コピーは日本語。`<title>` は `NAME ─ 日本語サブタイトル`。
- 開始オーバーレイの中で AudioContext を生成／再開する。
- `⟲ カメラ` ボタンと、getUserMedia 失敗時の日本語メッセージ:
  `カメラを起動できませんでした：<reason>` +
  `HTTPS または localhost で開き、カメラの使用を許可してください。`
- ランドマークは必ずガードする（配列の存在・長さ・該当インデックスの非 null）。
- 作品ごとに**別々のアートディレクション**を持たせる。共通スキンは使わない。
  ファイル冒頭のコメントにパレット／背景／書体／モーション／シグネチャを書く。
